import TransactionsList from "./ui/TransactionsList";

const Transactions = () => {
  return (
    <div className="text-left">
      <TransactionsList />
    </div>
  );
};

export default Transactions;
